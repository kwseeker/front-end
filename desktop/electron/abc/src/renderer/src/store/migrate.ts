// import { createMigrate } from 'redux-persist'

// const migrateConfig = {}

// // migrate 可以是一个接收当前 state 并返回一个用于返回新状态对象的 Promise 的函数
// // 应用场景：持久化的状态数据结构变更不兼容时，需要将状态数据从旧的数据结构迁移到新的数据结构
// const migrate = createMigrate(migrateConfig as any)

// export default migrate
